import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ca.findmyhelper.www',
  appName: 'mobile_applicatiion',
  webDir: 'dist/public'
};

export default config;
